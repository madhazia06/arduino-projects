# LED Blink

A simple Arduino program that makes the built-in LED on **pin 13** blink every second.

## What I learned

- `pinMode()` — sets the LED pin as an output.
- `digitalWrite()` — turns the LED ON/OFF.
- `delay()` — creates a 1-second pause between states.
